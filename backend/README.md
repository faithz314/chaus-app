Backend API: FastAPI official docs
Database: PostgreSQL official site
ORM: SQLAlchemy (Object Relational Mapper) => maps database tables to Python classes and database rows to objects
DB migrations: Alembic


Run the server:
```uvicorn app.main:app --reload```

Visit:
```uvicorn app.main:app --reload```
Visit for docs:
```http://127.0.0.1:8000/docs```